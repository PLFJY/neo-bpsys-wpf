You are working on repository:

PLFJY/neo-bpsys-wpf

Branch:
dev-refactor/Designer-v3

Task:
Implement Phase 14C:
Move FrontedDesignerWindow zoom controls to the bottom status bar.

Current state:
- Top toolbar still shows a ZoomDisplay TextBlock.
- View menu already contains Fit / ZoomIn / ZoomOut / zoom preset menu items.
- Bottom status bar currently shows:
  - left status button
  - right current window/canvas display
- ViewModel already has ZoomScale, ZoomDisplay, IsFitMode, ZoomPresets, SelectedZoomPreset, FitToWindowCommand, SetZoomPresetCommand, ZoomInCommand, ZoomOutCommand.

Goal:
Move visible zoom controls to bottom-left, immediately to the right of the status button.

Desired bottom status bar order:
<Status button> <Editable ComboBox for zoom> <Slider> <Fit Button> ... <right-aligned current Window/Canvas text>

Top toolbar:
- remove the standalone ZoomDisplay TextBlock from the top toolbar.
- keep the View menu zoom commands and preset menu items.

Do NOT:
- Do not change renderer architecture.
- Do not re-render canvas when only zoom scale changes.
- Do not break Ctrl + MouseWheel zoom.
- Do not break View menu zoom commands.
- Do not break FitToWindow behavior.
- Do not break existing ZoomScale LayoutTransform binding.
- Do not break 14A performance optimizations.
- Do not break 14B layer panel.
- Do not change `.bpui` schema.
- Do not touch plugin import/export.

Files to inspect:
- neo-bpsys-wpf/Views/Windows/FrontedDesignerWindow.xaml
- neo-bpsys-wpf/Views/Windows/FrontedDesignerWindow.xaml.cs
- neo-bpsys-wpf/ViewModels/Windows/FrontedDesignerWindowViewModel.cs
- localization files:
  - Lang.resx
  - Lang.en-us.resx
  - Lang.ja-jp.resx
- tests touching FrontedDesignerWindow XAML / zoom behavior

Part A: Move visible zoom UI to bottom status bar

Current bottom status bar is Grid.Row=3 with DockPanel:
- Status button docked left
- CurrentWindowCanvasDisplay text right

Change it to:
- status button on the left
- zoom ComboBox to the right of status button
- zoom Slider after ComboBox
- Fit button after Slider
- CurrentWindowCanvasDisplay remains right-aligned

Suggested layout:
Use Grid or DockPanel with a left StackPanel and right TextBlock.

Example structure:
<Grid>
  <Grid.ColumnDefinitions>
    <ColumnDefinition Width="Auto"/>
    <ColumnDefinition Width="*"/>
    <ColumnDefinition Width="Auto"/>
  </Grid.ColumnDefinitions>

  <StackPanel Grid.Column="0" Orientation="Horizontal">
    <ui:Button Content="{Binding StatusMessage}" ... />
    <ComboBox ... />
    <Slider ... />
    <ui:Button Command="{Binding FitToWindowCommand}" Content="{lex:Loc Fit}" />
  </StackPanel>

  <TextBlock Grid.Column="2" Text="{Binding CurrentWindowCanvasDisplay}" />
</Grid>

Keep status button as the left-most item.
Do not make the status text too wide; use reasonable MaxWidth/TextTrimming if necessary.

Part B: Remove top toolbar ZoomDisplay TextBlock

Top toolbar currently has a TextBlock bound to ZoomDisplay.
Remove that top-level visible TextBlock.

Keep:
- View menu Fit/ZoomIn/ZoomOut.
- View menu zoom presets.
- SnapStatusText.
- DirtyIndicatorText.
- LayoutSourcePath.

Part C: Editable zoom ComboBox

Add an editable ComboBox in bottom bar.

Requirements:
- Shows current zoom text.
- User can type:
  - `100`
  - `100%`
  - `1x`
  - `1.25x`
  - `Fit`
- Press Enter or lose focus commits typed value.
- Dropdown contains presets:
  - Fit
  - 25%
  - 50%
  - 75%
  - 100%
  - 125%
  - 150%
  - 200%
- Selecting a preset applies it.
- Editing a manual value exits Fit mode.
- Invalid input should not crash.
- Invalid input should restore current ZoomDisplay and optionally set StatusMessage.
- Clamp to current allowed range:
  - minimum 25%
  - maximum 200%
unless existing ViewModel already defines a different range.

Implementation options:
1. Add ViewModel property:
   - ZoomEditText
   - ZoomPercentValue
   - CommitZoomTextCommand
   - ApplyZoomPercentCommand
2. Or handle ComboBox events in code-behind and call ViewModel methods.

Prefer ViewModel methods for testability:
- `bool TryApplyZoomText(string? text)`
- `void ApplyZoomPercent(double percent)`
- `void UpdateZoomEditTextFromCurrentZoom()`

Ensure ZoomEditText updates when:
- ZoomScale changes.
- FitToWindow recalculates zoom.
- menu preset changes zoom.
- Ctrl+MouseWheel changes zoom.

Part D: Zoom Slider

Add a Slider after ComboBox.

Requirements:
- Min = 25
- Max = 200
- SmallChange = 5
- LargeChange = 25
- TickFrequency = 25 or 5
- Value = ZoomScale * 100
- Dragging slider updates ZoomScale live.
- Slider update exits Fit mode.
- Slider should not cause full preview re-render; it should only update zoom transform.
- Slider and ComboBox must stay in sync.

Implementation:
- Add ViewModel property `ZoomPercent`.
- Getter returns ZoomScale * 100.
- Setter clamps value, updates ZoomScale = value / 100, sets IsFitMode = false, updates ZoomDisplay/ZoomEditText.
- Avoid recursive property change loops.
- Raise OnPropertyChanged for ZoomPercent when ZoomScale changes.

Part E: Fit Button behavior

Add a bottom Fit button after the slider.

Requirements:
- Command = FitToWindowCommand.
- Tooltip = Fit.
- Button can use text "Fit" or icon + text.
- Clicking Fit:
  - sets IsFitMode = true.
  - recalculates ZoomScale based on viewport/canvas.
  - updates ComboBox text.
  - updates Slider value.
- If Fit mode displays something like `Fit (87%)`, keep it readable.
- If current ZoomDisplay is just `Fit`, consider adding a secondary computed text or use ZoomEditText to show actual percent.

Do not break existing SizeChanged fit recalculation.

Part F: Existing input gestures

Ensure existing controls remain functional:
- Ctrl + MouseWheel zoom still works.
- View menu preset zoom still works.
- View menu Fit still works.
- ZoomIn/ZoomOut commands still work.

If those paths currently call ApplyManualZoom / ApplyZoomPreset:
- make sure they update ZoomPercent and ZoomEditText notifications.

Part G: Localization

Add localization keys if missing:
- Designer.Zoom
- Designer.ZoomPercent
- Designer.ZoomInvalid
- Fit tooltip can reuse existing Fit key.
- ZoomIn/ZoomOut can reuse existing keys.

Add to:
- Lang.resx
- Lang.en-us.resx
- Lang.ja-jp.resx

Do not translate raw percent values.

Part H: Visual details

Bottom bar should remain compact:
- ComboBox width around 86-100.
- Slider width around 140-180.
- Fit button compact.
- Align vertically center.
- Keep status button left-most.
- Keep current window/canvas right-aligned.
- If window is narrow, zoom controls should not completely crush status text; use MaxWidth/TextTrimming.

Part I: Tests

Add/update tests:

XAML structure:
1. Top toolbar no longer contains the standalone ZoomDisplay TextBlock.
2. Bottom status bar contains zoom ComboBox.
3. Bottom status bar contains zoom Slider.
4. Bottom status bar contains Fit button.
5. View menu zoom commands still exist.

ViewModel:
6. TryApplyZoomText parses `100`.
7. TryApplyZoomText parses `100%`.
8. TryApplyZoomText parses `1x`.
9. TryApplyZoomText parses `1.25x`.
10. TryApplyZoomText handles `Fit`.
11. invalid zoom text does not change ZoomScale and returns false.
12. ZoomPercent setter updates ZoomScale.
13. ZoomScale change updates ZoomPercent notification.
14. FitToWindow updates ZoomEditText / ZoomPercent.
15. ZoomIn/ZoomOut update ZoomEditText / ZoomPercent.

Regression:
16. Ctrl+MouseWheel path still calls zoom logic.
17. View menu preset SetZoomPreset still works.
18. IsFitMode becomes false after manual ComboBox/Slider zoom.
19. IsFitMode remains true after Fit.

Part J: Manual validation

Run:
- git diff --check
- dotnet build
- dotnet test

Manual:
1. Open FrontedDesignerWindow.
2. Confirm visible zoom controls are now bottom-left:
   Status -> ComboBox -> Slider -> Fit.
3. Confirm top toolbar no longer has ZoomDisplay text.
4. View menu zoom commands still exist.
5. Type `100` in ComboBox and press Enter; zoom becomes 100%.
6. Type `150%`; zoom becomes 150%.
7. Type `1.25x`; zoom becomes 125%.
8. Select 75% from dropdown.
9. Drag slider; canvas zooms live.
10. Click Fit; canvas fits viewport.
11. Resize window; if Fit mode is active, zoom updates.
12. Ctrl + MouseWheel still zooms.
13. Layer panel from 14B still works.
14. Paste/delete/undo/redo from 14A still feels responsive.

Final response:
- summarize bottom status bar zoom UI
- summarize ComboBox parsing
- summarize Slider behavior
- summarize Fit behavior
- confirm top ZoomDisplay removed but View menu retained
- report build/test result