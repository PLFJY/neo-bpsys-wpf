# 版本号迭代原则

- 首位在出现大型重构或重大更改时（如官方规则改变、UI 重构）跟进。
- 第二位出现重大的新 Module 更新 / 第三位满十跟进。
- 第三位是有新 Feature 就会跟进。
- 最后由构建时传入，为 git 的提交号，本地构建为 `local`。

详细版本概念（应用版本、BuildMeta、插件 API 版本等）见 [build-release-and-versioning.md](build-release-and-versioning.md) 的「版本概念」章节。
