<controls:FrontedWindowBase
    x:Class="neo_bpsys_wpf.Views.Windows.WidgetsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:neo_bpsys_wpf.Core.Controls;assembly=neo-bpsys-wpf.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:neo_bpsys_wpf.ViewModels.Windows"
    Title="{lex:Loc FrontendWidgets}"
    Width="{Binding Settings.WindowSize.Width, Mode=TwoWay}"
    Height="{Binding Settings.WindowSize.Height, Mode=TwoWay}"
    d:DataContext="{d:DesignInstance Type=vm:WidgetsWindowViewModel,
                                     IsDesignTimeCreatable=True}"
    AllowsTransparency="{Binding Settings.AllowsWindowTransparency, Mode=OneTime}"
    Background="{Binding Settings.BackgroundBrush}"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid Width="1440" Height="716">
        <Grid.RowDefinitions>
            <RowDefinition Height="554" />
            <RowDefinition Height="160" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Height="554">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="308" />
                <ColumnDefinition Width="1132" />
            </Grid.ColumnDefinitions>
            <Canvas
                x:Name="MapBpCanvas"
                Width="308"
                Height="554" />
            <Canvas
                x:Name="BpOverViewCanvas"
                Grid.Column="1"
                Width="1132"
                Height="182"
                VerticalAlignment="Top" />
        </Grid>
        <Canvas
            x:Name="MapV2Canvas"
            Grid.Row="1"
            Width="1440"
            Height="160" />
    </Grid>
</controls:FrontedWindowBase>
