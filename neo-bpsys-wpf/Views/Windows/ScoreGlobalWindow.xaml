<controls:FrontedWindowBase
    x:Class="neo_bpsys_wpf.Views.Windows.ScoreGlobalWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:neo_bpsys_wpf.Core.Controls;assembly=neo-bpsys-wpf.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:neo_bpsys_wpf.ViewModels.Windows"
    Title="{lex:Loc FrontedScoreStatistics}"
    Width="{Binding Settings.ScoreGlobalWindowSize.Width, Mode=TwoWay}"
    Height="{Binding Settings.ScoreGlobalWindowSize.Height, Mode=TwoWay}"
    d:DataContext="{d:DesignInstance Type=vm:ScoreWindowViewModel,
                                     IsDesignTimeCreatable=True}"
    AllowsTransparency="{Binding Settings.AllowsScoreGlobalWindowTransparency, Mode=OneTime}"
    Background="{Binding Settings.ScoreGlobalWindowBackgroundBrush}"
    WindowStyle="None"
    mc:Ignorable="d">
    <Canvas
        Name="BaseCanvas"
        Width="1440"
        Height="195" />
</controls:FrontedWindowBase>
