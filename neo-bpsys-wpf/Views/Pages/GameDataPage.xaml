<Page
    x:Class="neo_bpsys_wpf.Views.Pages.GameDataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cc="clr-namespace:neo_bpsys_wpf.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:neo_bpsys_wpf.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:neo_bpsys_wpf.ViewModels.Pages"
    Title="GameDataPage"
    d:DataContext="{d:DesignInstance Type=vm:GameDataPageViewModel, IsDesignTimeCreatable=True}"
    d:DesignHeight="1500"
    d:DesignWidth="685"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="求生者" />
        <WrapPanel Grid.Row="1" Margin="0,10,0,0">
            <Border Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <ui:TextBox
                        Grid.Row="0"
                        IsReadOnly="True"
                        PlaceholderText="角色名称" />
                    <StackPanel Grid.Row="1">
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="破译进度" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="砸板命中次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="救人次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="治疗次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="牵制监管者时间" />
                    </StackPanel>
                </Grid>
            </Border>
            <Border Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <ui:TextBox
                        Grid.Row="0"
                        IsReadOnly="True"
                        PlaceholderText="角色名称" />
                    <StackPanel Grid.Row="1">
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="破译进度" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="砸板命中次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="救人次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="治疗次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="牵制监管者时间" />
                    </StackPanel>
                </Grid>
            </Border>
            <Border Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <ui:TextBox
                        Grid.Row="0"
                        IsReadOnly="True"
                        PlaceholderText="角色名称" />
                    <StackPanel Grid.Row="1">
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="破译进度" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="砸板命中次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="救人次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="治疗次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="牵制监管者时间" />
                    </StackPanel>
                </Grid>
            </Border>
            <Border Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <ui:TextBox
                        Grid.Row="0"
                        IsReadOnly="True"
                        PlaceholderText="角色名称" />
                    <StackPanel Grid.Row="1">
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="破译进度" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="砸板命中次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="救人次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="治疗次数" />
                        <cc:GameDataEditor Margin="0,10,0,0" GameDataType="牵制监管者时间" />
                    </StackPanel>
                </Grid>
            </Border>
        </WrapPanel>
        <TextBlock Grid.Row="2" Text="监管者" />
        <Border
            Grid.Row="3"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ui:TextBox Grid.Row="0" PlaceholderText="角色名称" />
                <StackPanel Grid.Row="1">
                    <cc:GameDataEditor Margin="0,10,0,0" GameDataType="剩余密码机数量" />
                    <cc:GameDataEditor Margin="0,10,0,0" GameDataType="破坏板子数" />
                    <cc:GameDataEditor Margin="0,10,0,0" GameDataType="命中求生者次数" />
                    <cc:GameDataEditor Margin="0,10,0,0" GameDataType="恐惧震慑次数" />
                    <cc:GameDataEditor Margin="0,10,0,0" GameDataType="击倒次数" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>


</Page>
